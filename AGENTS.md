# weight-agent

## Cursor Cloud specific instructions

This repository is currently in its initial state with only a `README.md`. There are no application services, dependencies, build systems, or test suites configured yet.

- **No package manager or dependency files** (`package.json`, `requirements.txt`, `pyproject.toml`, etc.) exist.
- **No services** need to be started.
- **No lint, test, or build commands** are available.

When source code and dependency files are added, update this section with:
- How to install dependencies (e.g. `npm install`, `pip install -r requirements.txt`)
- How to run lint, tests, and the dev server
- Any non-obvious startup caveats
